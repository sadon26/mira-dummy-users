export type HeaderItem = {
  key: string;
  text: string;
  class?: string;
};

export type TableDataItem = {
  key: string;
  text: string;
};

export type RowType = {
  firstName: string;
  lastName: string;
  email: string;
  image: string;
  birthDate: string;
};
